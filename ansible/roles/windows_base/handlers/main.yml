---
# =======================================================================
# Role: windows_base — handlers/main.yml
# =======================================================================

- name: reboot windows host
  ansible.windows.win_reboot:
    reboot_timeout: 300
    post_reboot_delay: 30
